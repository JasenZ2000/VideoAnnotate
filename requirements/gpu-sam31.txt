# Install this inside the existing ComfyUI/SAM3.1 environment.
# PyTorch is intentionally omitted because its build must match the server CUDA runtime.
fastapi>=0.100
uvicorn[standard]>=0.20
opencv-python-headless>=4.8
numpy>=1.24
pydantic>=2.7
